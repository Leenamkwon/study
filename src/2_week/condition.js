const SALE = 'COUPON'

if (SALE === 'COUPON') {
  return '쿠폰 할인'
} else if (SALE === 'SPECIAL') {
  return '특별 할인'
} else {
  return '할인'
}

switch (SLAE) {
  case 'COUPON': {
    return '쿠폰 할인'
    break
  }
  case 'SPECIAL': {
    return '특별 할인'
    break
  }
}
